## Agent skills

### Issue tracker

Issues are tracked using local markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout with `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
